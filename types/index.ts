export type Country = {
  alt: { alt: string};
  name: { common: string };
  nameOfficial: { official: string };
  flag: { png: string};
  population: number;
  region: string;
  subregion: string;
  capital: string[];
}